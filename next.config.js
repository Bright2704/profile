/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['4.bp.blogspot.com'], // เพิ่ม hostname ของรูปภาพของคุณที่นี่
        
    },
};

module.exports = nextConfig
